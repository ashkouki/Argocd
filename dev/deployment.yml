apiVersion: apps/v1
kind: Deployment
metadata:
  name:  myapps
spec:
  replicas: 3
  selector:
    matchLabels:
      app:  myapps
  template:
    metadata:
      labels:
        app: web-app
    spec:
      containers:
      - name:  myapps
        image: nginx:stable-alpine3.17
        ports:
        - containerPort: 80
